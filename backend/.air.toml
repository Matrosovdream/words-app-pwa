root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/server ./cmd/web"
  bin = "./tmp/server"
  include_ext = ["go", "json"]
  exclude_dir = ["tmp", "dist", "data"]
  delay = 300
  stop_on_error = true

[log]
  time = true

[misc]
  clean_on_exit = true
